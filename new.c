#include<stdio.h>

int main()

{
    int x,y;

    scanf("%d %d",&x,&y);

    if(x>y)

    printf("%d is the bigger than %d",x,y);

    else
    printf("%d is the bigger than %d",y,x);


    return 0;
}
